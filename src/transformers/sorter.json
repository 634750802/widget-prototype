{
  "$schema": "http://json-schema.org/draft-04/schema",
  "title": "SorterTransformer",
  "type": "object",
  "properties": {
    "type": {
      "enum": ["sorter"]
    },
    "path": {
      "type": "string"
    },
    "order": {
      "type": "string",
      "enum": ["asc", "desc"]
    }
  },
  "additionalProperties": false,
  "required": ["type", "path"]
}